#include "../Output.h"
/*
环形链表 II

给定一个链表，返回链表开始入环的第一个节点。 如果链表无环，则返回 null。

说明：不允许修改给定的链表。

进阶：
你是否可以不用额外空间解决此题？

 */
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
    
    }
};
int main() {
    problem(__FILE__,"");
    Solution solution;
    ListNode* x=inPut({1,2,3});
    outPut(x);
    int xx=x->val;
    outPut();
    return 0;
}